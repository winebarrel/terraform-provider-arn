---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "pi_perf_reports_resource function - arn"
subcategory: ""
description: |-
  pi perf-reports-resource ARN
---

# function: pi_perf_reports_resource

Builds the ARN for `pi` `perf-reports-resource`:

```
arn:${Partition}:pi:${Region}:${Account}:perf-reports/${ServiceType}/${Identifier}/${ReportId}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
pi_perf_reports_resource(service_type string, identifier string, report_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `service_type` (String) Value for `${ServiceType}` in the ARN template.
1. `identifier` (String) Value for `${Identifier}` in the ARN template.
1. `report_id` (String) Value for `${ReportId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

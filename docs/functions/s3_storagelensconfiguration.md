---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "s3_storagelensconfiguration function - arn"
subcategory: ""
description: |-
  s3 storagelensconfiguration ARN
---

# function: s3_storagelensconfiguration

Builds the ARN for `s3` `storagelensconfiguration`:

```
arn:${Partition}:s3:${Region}:${Account}:storage-lens/${ConfigId}
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.

## Example Usage

```terraform
# arn:aws:s3:ap-northeast-1:111111111111:storage-lens/config-id
output "s3_storagelensconfiguration" {
  value = provider::arn::s3_storagelensconfiguration("config-id")
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
s3_storagelensconfiguration(config_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `config_id` (String) Value for `${ConfigId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

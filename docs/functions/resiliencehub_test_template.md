---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "resiliencehub_test_template function - arn"
subcategory: ""
description: |-
  Builds an resiliencehub test-template ARN
---

# function: resiliencehub_test_template

Builds the ARN of an `resiliencehub` `test-template`:

```
arn:${Partition}:resiliencehub:${Region}:${Account}:test-template/${TestTemplateId}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
resiliencehub_test_template(test_template_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `test_template_id` (String) Value for `${TestTemplateId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given.

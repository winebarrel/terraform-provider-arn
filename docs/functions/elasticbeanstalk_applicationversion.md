---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "elasticbeanstalk_applicationversion function - arn"
subcategory: ""
description: |-
  elasticbeanstalk applicationversion ARN
---

# function: elasticbeanstalk_applicationversion

Builds the ARN for `elasticbeanstalk` `applicationversion`:

```
arn:${Partition}:elasticbeanstalk:${Region}:${Account}:applicationversion/${ApplicationName}/${VersionLabel}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
elasticbeanstalk_applicationversion(application_name string, version_label string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `application_name` (String) Value for `${ApplicationName}` in the ARN template.
1. `version_label` (String) Value for `${VersionLabel}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

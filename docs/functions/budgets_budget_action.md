---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "budgets_budget_action function - arn"
subcategory: ""
description: |-
  budgets budgetAction ARN
---

# function: budgets_budget_action

Builds the ARN for `budgets` `budgetAction`:

```
arn:${Partition}:budgets::${Account}:budget/${BudgetName}/action/${ActionId}
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.

## Example Usage

```terraform
# arn:aws:budgets::111111111111:budget/budget-name/action/action-id
output "budgets_budget_action" {
  value = provider::arn::budgets_budget_action("budget-name", "action-id")
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
budgets_budget_action(budget_name string, action_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `budget_name` (String) Value for `${BudgetName}` in the ARN template.
1. `action_id` (String) Value for `${ActionId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "nimble_streaming_image function - arn"
subcategory: ""
description: |-
  nimble streaming-image ARN
---

# function: nimble_streaming_image

Builds the ARN for `nimble` `streaming-image`:

```
arn:${Partition}:nimble:${Region}:${Account}:streaming-image/${StreamingImageId}
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.

## Example Usage

```terraform
# arn:aws:nimble:ap-northeast-1:111111111111:streaming-image/streaming-image-id
output "nimble_streaming_image" {
  value = provider::arn::nimble_streaming_image("streaming-image-id")
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
nimble_streaming_image(streaming_image_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `streaming_image_id` (String) Value for `${StreamingImageId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

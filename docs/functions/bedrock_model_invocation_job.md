---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "bedrock_model_invocation_job function - arn"
subcategory: ""
description: |-
  bedrock model-invocation-job ARN
---

# function: bedrock_model_invocation_job

Builds the ARN for `bedrock` `model-invocation-job`:

```
arn:${Partition}:bedrock:${Region}:${Account}:model-invocation-job/${JobIdentifier}
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.

## Example Usage

```terraform
# arn:aws:bedrock:ap-northeast-1:111111111111:model-invocation-job/job-identifier
output "bedrock_model_invocation_job" {
  value = provider::arn::bedrock_model_invocation_job("job-identifier")
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
bedrock_model_invocation_job(job_identifier string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `job_identifier` (String) Value for `${JobIdentifier}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "emr_containers_job_run function - arn"
subcategory: ""
description: |-
  Builds an emr-containers jobRun ARN
---

# function: emr_containers_job_run

Builds the ARN of an `emr-containers` `jobRun`:

```
arn:${Partition}:emr-containers:${Region}:${Account}:/virtualclusters/${VirtualClusterId}/jobruns/${JobRunId}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
emr_containers_job_run(virtual_cluster_id string, job_run_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `virtual_cluster_id` (String) Value for `${VirtualClusterId}` in the ARN template.
1. `job_run_id` (String) Value for `${JobRunId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given.

---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "appconfig_deployment function - arn"
subcategory: ""
description: |-
  appconfig deployment ARN
---

# function: appconfig_deployment

Builds the ARN for `appconfig` `deployment`:

```
arn:${Partition}:appconfig:${Region}:${Account}:application/${ApplicationId}/environment/${EnvironmentId}/deployment/${DeploymentNumber}
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
appconfig_deployment(application_id string, environment_id string, deployment_number string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `application_id` (String) Value for `${ApplicationId}` in the ARN template.
1. `environment_id` (String) Value for `${EnvironmentId}` in the ARN template.
1. `deployment_number` (String) Value for `${DeploymentNumber}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

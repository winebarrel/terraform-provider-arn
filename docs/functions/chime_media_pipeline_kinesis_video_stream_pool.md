---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "chime_media_pipeline_kinesis_video_stream_pool function - arn"
subcategory: ""
description: |-
  Builds an chime media-pipeline-kinesis-video-stream-pool ARN
---

# function: chime_media_pipeline_kinesis_video_stream_pool

Builds the ARN of an `chime` `media-pipeline-kinesis-video-stream-pool`:

```
arn:${Partition}:chime:${Region}:${AccountId}:media-pipeline-kinesis-video-stream-pool/${PoolName}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
chime_media_pipeline_kinesis_video_stream_pool(account_id string, pool_name string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `account_id` (String) Value for `${AccountId}` in the ARN template.
1. `pool_name` (String) Value for `${PoolName}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "cognito_sync_dataset function - arn"
subcategory: ""
description: |-
  cognito-sync dataset ARN
---

# function: cognito_sync_dataset

Builds the ARN for `cognito-sync` `dataset`:

```
arn:${Partition}:cognito-sync:${Region}:${Account}:identitypool/${IdentityPoolId}/identity/${IdentityId}/dataset/${DatasetName}
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.

## Example Usage

```terraform
# arn:aws:cognito-sync:ap-northeast-1:111111111111:identitypool/identity-pool-id/identity/identity-id/dataset/dataset-name
output "cognito_sync_dataset" {
  value = provider::arn::cognito_sync_dataset("identity-pool-id", "identity-id", "dataset-name")
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
cognito_sync_dataset(identity_pool_id string, identity_id string, dataset_name string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `identity_pool_id` (String) Value for `${IdentityPoolId}` in the ARN template.
1. `identity_id` (String) Value for `${IdentityId}` in the ARN template.
1. `dataset_name` (String) Value for `${DatasetName}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

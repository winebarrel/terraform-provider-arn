---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "rekognition_dataset function - arn"
subcategory: ""
description: |-
  Builds an rekognition dataset ARN
---

# function: rekognition_dataset

Builds the ARN of an `rekognition` `dataset`:

```
arn:${Partition}:rekognition:${Region}:${Account}:project/${ProjectName}/dataset/${DatasetType}/${CreationTimestamp}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
rekognition_dataset(project_name string, dataset_type string, creation_timestamp string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `project_name` (String) Value for `${ProjectName}` in the ARN template.
1. `dataset_type` (String) Value for `${DatasetType}` in the ARN template.
1. `creation_timestamp` (String) Value for `${CreationTimestamp}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

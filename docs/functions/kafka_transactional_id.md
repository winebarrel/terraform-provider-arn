---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "kafka_transactional_id function - arn"
subcategory: ""
description: |-
  kafka transactional-id ARN
---

# function: kafka_transactional_id

Builds the ARN for `kafka` `transactional-id`:

```
arn:${Partition}:kafka:${Region}:${Account}:transactional-id/${ClusterName}/${ClusterUuid}/${TransactionalId}
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.

## Example Usage

```terraform
# arn:aws:kafka:ap-northeast-1:111111111111:transactional-id/cluster-name/cluster-uuid/transactional-id
output "kafka_transactional_id" {
  value = provider::arn::kafka_transactional_id("cluster-name", "cluster-uuid", "transactional-id")
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
kafka_transactional_id(cluster_name string, cluster_uuid string, transactional_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `cluster_name` (String) Value for `${ClusterName}` in the ARN template.
1. `cluster_uuid` (String) Value for `${ClusterUuid}` in the ARN template.
1. `transactional_id` (String) Value for `${TransactionalId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

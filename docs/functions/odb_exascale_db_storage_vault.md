---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "odb_exascale_db_storage_vault function - arn"
subcategory: ""
description: |-
  Builds an odb exascale-db-storage-vault ARN
---

# function: odb_exascale_db_storage_vault

Builds the ARN of an `odb` `exascale-db-storage-vault`:

```
arn:${Partition}:odb:${Region}:${Account}:exascale-db-storage-vault/${ExascaleDbStorageVaultId}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
odb_exascale_db_storage_vault(exascale_db_storage_vault_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `exascale_db_storage_vault_id` (String) Value for `${ExascaleDbStorageVaultId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given.

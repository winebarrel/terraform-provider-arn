---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "chime_media_pipeline function - arn"
subcategory: ""
description: |-
  chime media-pipeline ARN
---

# function: chime_media_pipeline

Builds the ARN for `chime` `media-pipeline`:

```
arn:${Partition}:chime:${Region}:${AccountId}:media-pipeline/${MediaPipelineId}
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.

## Example Usage

```terraform
# arn:aws:chime:ap-northeast-1:111111111111:media-pipeline/media-pipeline-id
output "chime_media_pipeline" {
  value = provider::arn::chime_media_pipeline("media-pipeline-id")
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
chime_media_pipeline(media_pipeline_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `media_pipeline_id` (String) Value for `${MediaPipelineId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

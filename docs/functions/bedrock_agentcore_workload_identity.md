---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "bedrock_agentcore_workload_identity function - arn"
subcategory: ""
description: |-
  Builds an bedrock-agentcore workload-identity ARN
---

# function: bedrock_agentcore_workload_identity

Builds the ARN of an `bedrock-agentcore` `workload-identity`:

```
arn:${Partition}:bedrock-agentcore:${Region}:${Account}:workload-identity-directory/${DirectoryId}/workload-identity/${WorkloadIdentityName}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
bedrock_agentcore_workload_identity(directory_id string, workload_identity_name string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `directory_id` (String) Value for `${DirectoryId}` in the ARN template.
1. `workload_identity_name` (String) Value for `${WorkloadIdentityName}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

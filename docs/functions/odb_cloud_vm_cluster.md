---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "odb_cloud_vm_cluster function - arn"
subcategory: ""
description: |-
  odb cloud-vm-cluster ARN
---

# function: odb_cloud_vm_cluster

Builds the ARN for `odb` `cloud-vm-cluster`:

```
arn:${Partition}:odb:${Region}:${Account}:cloud-vm-cluster/${CloudVmClusterId}
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.

## Example Usage

```terraform
# arn:aws:odb:ap-northeast-1:111111111111:cloud-vm-cluster/cloud-vm-cluster-id
output "odb_cloud_vm_cluster" {
  value = provider::arn::odb_cloud_vm_cluster("cloud-vm-cluster-id")
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
odb_cloud_vm_cluster(cloud_vm_cluster_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `cloud_vm_cluster_id` (String) Value for `${CloudVmClusterId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

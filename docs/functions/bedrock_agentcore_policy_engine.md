---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "bedrock_agentcore_policy_engine function - arn"
subcategory: ""
description: |-
  bedrock-agentcore policy-engine ARN
---

# function: bedrock_agentcore_policy_engine

Builds the ARN for `bedrock-agentcore` `policy-engine`:

```
arn:${Partition}:bedrock-agentcore:${Region}:${Account}:policy-engine/${PolicyEngineId}
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.

## Example Usage

```terraform
# arn:aws:bedrock-agentcore:ap-northeast-1:111111111111:policy-engine/policy-engine-id
output "bedrock_agentcore_policy_engine" {
  value = provider::arn::bedrock_agentcore_policy_engine("policy-engine-id")
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
bedrock_agentcore_policy_engine(policy_engine_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `policy_engine_id` (String) Value for `${PolicyEngineId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

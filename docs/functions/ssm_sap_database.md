---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "ssm_sap_database function - arn"
subcategory: ""
description: |-
  ssm-sap database ARN
---

# function: ssm_sap_database

Builds the ARN for `ssm-sap` `database`:

```
arn:${Partition}:ssm-sap:${Region}:${Account}:${ApplicationType}/${ApplicationId}/DB/${DatabaseId}
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.

## Example Usage

```terraform
# arn:aws:ssm-sap:ap-northeast-1:111111111111:application-type/application-id/DB/database-id
output "ssm_sap_database" {
  value = provider::arn::ssm_sap_database("application-type", "application-id", "database-id")
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
ssm_sap_database(application_type string, application_id string, database_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `application_type` (String) Value for `${ApplicationType}` in the ARN template.
1. `application_id` (String) Value for `${ApplicationId}` in the ARN template.
1. `database_id` (String) Value for `${DatabaseId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

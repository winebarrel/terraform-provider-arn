---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "odb_cloud_autonomous_vm_cluster function - arn"
subcategory: ""
description: |-
  Builds an odb cloud-autonomous-vm-cluster ARN
---

# function: odb_cloud_autonomous_vm_cluster

Builds the ARN of an `odb` `cloud-autonomous-vm-cluster`:

```
arn:${Partition}:odb:${Region}:${Account}:cloud-autonomous-vm-cluster/${CloudAutonomousVmClusterId}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
odb_cloud_autonomous_vm_cluster(cloud_autonomous_vm_cluster_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `cloud_autonomous_vm_cluster_id` (String) Value for `${CloudAutonomousVmClusterId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given.

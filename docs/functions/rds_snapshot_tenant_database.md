---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "rds_snapshot_tenant_database function - arn"
subcategory: ""
description: |-
  Builds an rds snapshot-tenant-database ARN
---

# function: rds_snapshot_tenant_database

Builds the ARN of an `rds` `snapshot-tenant-database`:

```
arn:${Partition}:rds:${Region}:${Account}:snapshot-tenant-database:${SnapshotName}:${TenantResourceId}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
rds_snapshot_tenant_database(snapshot_name string, tenant_resource_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `snapshot_name` (String) Value for `${SnapshotName}` in the ARN template.
1. `tenant_resource_id` (String) Value for `${TenantResourceId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given.

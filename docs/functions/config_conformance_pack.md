---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "config_conformance_pack function - arn"
subcategory: ""
description: |-
  Builds an config ConformancePack ARN
---

# function: config_conformance_pack

Builds the ARN of an `config` `ConformancePack`:

```
arn:${Partition}:config:${Region}:${Account}:conformance-pack/${ConformancePackName}/${ConformancePackId}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
config_conformance_pack(conformance_pack_name string, conformance_pack_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `conformance_pack_name` (String) Value for `${ConformancePackName}` in the ARN template.
1. `conformance_pack_id` (String) Value for `${ConformancePackId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given.

---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "execute_api_execute_api_general function - arn"
subcategory: ""
description: |-
  execute-api execute-api-general ARN
---

# function: execute_api_execute_api_general

Builds the ARN for `execute-api` `execute-api-general`:

```
arn:${Partition}:execute-api:${Region}:${Account}:${ApiId}/${Stage}/${Method}/${ApiSpecificResourcePath}
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
execute_api_execute_api_general(api_id string, stage string, method string, api_specific_resource_path string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `api_id` (String) Value for `${ApiId}` in the ARN template.
1. `stage` (String) Value for `${Stage}` in the ARN template.
1. `method` (String) Value for `${Method}` in the ARN template.
1. `api_specific_resource_path` (String) Value for `${ApiSpecificResourcePath}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "bedrock_agent_alias function - arn"
subcategory: ""
description: |-
  bedrock agent-alias ARN
---

# function: bedrock_agent_alias

Builds the ARN for `bedrock` `agent-alias`:

```
arn:${Partition}:bedrock:${Region}:${Account}:agent-alias/${AgentId}/${AgentAliasId}
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.

## Example Usage

```terraform
# arn:aws:bedrock:ap-northeast-1:111111111111:agent-alias/agent-id/agent-alias-id
output "bedrock_agent_alias" {
  value = provider::arn::bedrock_agent_alias("agent-id", "agent-alias-id")
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
bedrock_agent_alias(agent_id string, agent_alias_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `agent_id` (String) Value for `${AgentId}` in the ARN template.
1. `agent_alias_id` (String) Value for `${AgentAliasId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

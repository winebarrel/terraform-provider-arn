---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "ssm_automation_definition function - arn"
subcategory: ""
description: |-
  ssm automation-definition ARN
---

# function: ssm_automation_definition

Builds the ARN for `ssm` `automation-definition`:

```
arn:${Partition}:ssm:${Region}:${Account}:automation-definition/${AutomationDefinitionName}:${VersionId}
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.

## Example Usage

```terraform
# arn:aws:ssm:ap-northeast-1:111111111111:automation-definition/automation-definition-name:version-id
output "ssm_automation_definition" {
  value = provider::arn::ssm_automation_definition("automation-definition-name", "version-id")
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
ssm_automation_definition(automation_definition_name string, version_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `automation_definition_name` (String) Value for `${AutomationDefinitionName}` in the ARN template.
1. `version_id` (String) Value for `${VersionId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

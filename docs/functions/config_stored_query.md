---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "config_stored_query function - arn"
subcategory: ""
description: |-
  Builds an config StoredQuery ARN
---

# function: config_stored_query

Builds the ARN of an `config` `StoredQuery`:

```
arn:${Partition}:config:${Region}:${Account}:stored-query/${StoredQueryName}/${StoredQueryId}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
config_stored_query(stored_query_name string, stored_query_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `stored_query_name` (String) Value for `${StoredQueryName}` in the ARN template.
1. `stored_query_id` (String) Value for `${StoredQueryId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given.

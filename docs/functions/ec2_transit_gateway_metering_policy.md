---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "ec2_transit_gateway_metering_policy function - arn"
subcategory: ""
description: |-
  Builds an ec2 transit-gateway-metering-policy ARN
---

# function: ec2_transit_gateway_metering_policy

Builds the ARN of an `ec2` `transit-gateway-metering-policy`:

```
arn:${Partition}:ec2:${Region}:${Account}:transit-gateway-metering-policy/${TransitGatewayMeteringPolicyId}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
ec2_transit_gateway_metering_policy(transit_gateway_metering_policy_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `transit_gateway_metering_policy_id` (String) Value for `${TransitGatewayMeteringPolicyId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

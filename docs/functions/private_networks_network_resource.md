---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "private_networks_network_resource function - arn"
subcategory: ""
description: |-
  private-networks network-resource ARN
---

# function: private_networks_network_resource

Builds the ARN for `private-networks` `network-resource`:

```
arn:${Partition}:private-networks:${Region}:${Account}:network-resource/${NetworkName}/${ResourceId}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
private_networks_network_resource(network_name string, resource_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `network_name` (String) Value for `${NetworkName}` in the ARN template.
1. `resource_id` (String) Value for `${ResourceId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

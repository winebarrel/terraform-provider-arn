---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "kafka_configuration function - arn"
subcategory: ""
description: |-
  kafka configuration ARN
---

# function: kafka_configuration

Builds the ARN for `kafka` `configuration`:

```
arn:${Partition}:kafka:${Region}:${Account}:configuration/${ConfigurationName}/${Uuid}
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.

## Example Usage

```terraform
# arn:aws:kafka:ap-northeast-1:111111111111:configuration/configuration-name/uuid
output "kafka_configuration" {
  value = provider::arn::kafka_configuration("configuration-name", "uuid")
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
kafka_configuration(configuration_name string, uuid string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `configuration_name` (String) Value for `${ConfigurationName}` in the ARN template.
1. `uuid` (String) Value for `${Uuid}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

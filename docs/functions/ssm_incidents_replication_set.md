---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "ssm_incidents_replication_set function - arn"
subcategory: ""
description: |-
  Builds an ssm-incidents replication-set ARN
---

# function: ssm_incidents_replication_set

Builds the ARN of an `ssm-incidents` `replication-set`:

```
arn:${Partition}:ssm-incidents::${Account}:replication-set/${ReplicationSet}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
ssm_incidents_replication_set(replication_set string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `replication_set` (String) Value for `${ReplicationSet}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given.

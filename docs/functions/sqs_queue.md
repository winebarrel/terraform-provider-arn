---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "sqs_queue function - arn"
subcategory: ""
description: |-
  Builds an sqs queue ARN
---

# function: sqs_queue

Builds the ARN of an `sqs` `queue`:

```
arn:${Partition}:sqs:${Region}:${Account}:${QueueName}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.

## Example Usage

```terraform
# arn:aws:sqs:ap-northeast-1:111111111111:my-queue
output "queue" {
  value = provider::arn::sqs_queue("my-queue")
}

# arn:aws:sqs:us-east-1:111111111111:my-queue
output "queue_in_us" {
  value = provider::arn::sqs_queue("my-queue", { region = "us-east-1" })
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
sqs_queue(queue_name string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `queue_name` (String) Value for `${QueueName}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

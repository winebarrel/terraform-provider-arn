---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "timestream_influxdb_db_instance function - arn"
subcategory: ""
description: |-
  timestream-influxdb db-instance ARN
---

# function: timestream_influxdb_db_instance

Builds the ARN for `timestream-influxdb` `db-instance`:

```
arn:${Partition}:timestream-influxdb:${Region}:${Account}:db-instance/${DbInstanceIdentifier}
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.

## Example Usage

```terraform
# arn:aws:timestream-influxdb:ap-northeast-1:111111111111:db-instance/db-instance-identifier
output "timestream_influxdb_db_instance" {
  value = provider::arn::timestream_influxdb_db_instance("db-instance-identifier")
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
timestream_influxdb_db_instance(db_instance_identifier string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `db_instance_identifier` (String) Value for `${DbInstanceIdentifier}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

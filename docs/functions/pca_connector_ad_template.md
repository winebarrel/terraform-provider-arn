---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "pca_connector_ad_template function - arn"
subcategory: ""
description: |-
  pca-connector-ad Template ARN
---

# function: pca_connector_ad_template

Builds the ARN for `pca-connector-ad` `Template`:

```
arn:${Partition}:pca-connector-ad:${Region}:${Account}:connector/${ConnectorId}/template/${TemplateId}
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.

## Example Usage

```terraform
# arn:aws:pca-connector-ad:ap-northeast-1:111111111111:connector/connector-id/template/template-id
output "pca_connector_ad_template" {
  value = provider::arn::pca_connector_ad_template("connector-id", "template-id")
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
pca_connector_ad_template(connector_id string, template_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `connector_id` (String) Value for `${ConnectorId}` in the ARN template.
1. `template_id` (String) Value for `${TemplateId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

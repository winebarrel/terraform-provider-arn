---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "lex_bot_alias_2 function - arn"
subcategory: ""
description: |-
  lex bot alias ARN
---

# function: lex_bot_alias_2

Builds the ARN for `lex` `bot alias`:

```
arn:${Partition}:lex:${Region}:${Account}:bot:${BotName}:${BotAlias}
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.

## Example Usage

```terraform
# arn:aws:lex:ap-northeast-1:111111111111:bot:bot-name:bot-alias
output "lex_bot_alias_2" {
  value = provider::arn::lex_bot_alias_2("bot-name", "bot-alias")
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
lex_bot_alias_2(bot_name string, bot_alias string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `bot_name` (String) Value for `${BotName}` in the ARN template.
1. `bot_alias` (String) Value for `${BotAlias}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

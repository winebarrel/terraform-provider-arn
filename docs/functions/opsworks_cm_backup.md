---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "opsworks_cm_backup function - arn"
subcategory: ""
description: |-
  opsworks-cm backup ARN
---

# function: opsworks_cm_backup

Builds the ARN for `opsworks-cm` `backup`:

```
arn:${Partition}:opsworks-cm::${Account}:backup/${ServerName}-{Date-and-Time-Stamp-of-Backup}
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.

## Example Usage

```terraform
# arn:aws:opsworks-cm::111111111111:backup/server-name-{Date-and-Time-Stamp-of-Backup}
output "opsworks_cm_backup" {
  value = provider::arn::opsworks_cm_backup("server-name")
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
opsworks_cm_backup(server_name string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `server_name` (String) Value for `${ServerName}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

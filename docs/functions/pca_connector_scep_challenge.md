---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "pca_connector_scep_challenge function - arn"
subcategory: ""
description: |-
  pca-connector-scep Challenge ARN
---

# function: pca_connector_scep_challenge

Builds the ARN for `pca-connector-scep` `Challenge`:

```
arn:${Partition}:pca-connector-scep:${Region}:${Account}:connector/${ConnectorId}/challenge/${ChallengeId}
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.

## Example Usage

```terraform
# arn:aws:pca-connector-scep:ap-northeast-1:111111111111:connector/connector-id/challenge/challenge-id
output "pca_connector_scep_challenge" {
  value = provider::arn::pca_connector_scep_challenge("connector-id", "challenge-id")
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
pca_connector_scep_challenge(connector_id string, challenge_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `connector_id` (String) Value for `${ConnectorId}` in the ARN template.
1. `challenge_id` (String) Value for `${ChallengeId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

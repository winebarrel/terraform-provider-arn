---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "apigateway_stage function - arn"
subcategory: ""
description: |-
  Builds an apigateway Stage ARN
---

# function: apigateway_stage

Builds the ARN of an `apigateway` `Stage`:

```
arn:${Partition}:apigateway:${Region}::/apis/${ApiId}/stages/${StageName}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
apigateway_stage(api_id string, stage_name string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `api_id` (String) Value for `${ApiId}` in the ARN template.
1. `stage_name` (String) Value for `${StageName}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given.

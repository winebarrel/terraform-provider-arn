---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "eks_identityproviderconfig function - arn"
subcategory: ""
description: |-
  Builds an eks identityproviderconfig ARN
---

# function: eks_identityproviderconfig

Builds the ARN of an `eks` `identityproviderconfig`:

```
arn:${Partition}:eks:${Region}:${Account}:identityproviderconfig/${ClusterName}/${IdentityProviderType}/${IdentityProviderConfigName}/${UUID}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
eks_identityproviderconfig(cluster_name string, identity_provider_type string, identity_provider_config_name string, uuid string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `cluster_name` (String) Value for `${ClusterName}` in the ARN template.
1. `identity_provider_type` (String) Value for `${IdentityProviderType}` in the ARN template.
1. `identity_provider_config_name` (String) Value for `${IdentityProviderConfigName}` in the ARN template.
1. `uuid` (String) Value for `${UUID}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given.

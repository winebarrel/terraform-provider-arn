---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "machinelearning_mlmodel function - arn"
subcategory: ""
description: |-
  machinelearning mlmodel ARN
---

# function: machinelearning_mlmodel

Builds the ARN for `machinelearning` `mlmodel`:

```
arn:${Partition}:machinelearning:${Region}:${Account}:mlmodel/${MlModelId}
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.

## Example Usage

```terraform
# arn:aws:machinelearning:ap-northeast-1:111111111111:mlmodel/ml-model-id
output "machinelearning_mlmodel" {
  value = provider::arn::machinelearning_mlmodel("ml-model-id")
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
machinelearning_mlmodel(ml_model_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `ml_model_id` (String) Value for `${MlModelId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

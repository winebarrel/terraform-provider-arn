---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "lambda_network_connector function - arn"
subcategory: ""
description: |-
  lambda networkConnector ARN
---

# function: lambda_network_connector

Builds the ARN for `lambda` `networkConnector`:

```
arn:${Partition}:lambda:${Region}:${Account}:network-connector:${NetworkConnectorId}
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.

## Example Usage

```terraform
# arn:aws:lambda:ap-northeast-1:111111111111:network-connector:network-connector-id
output "lambda_network_connector" {
  value = provider::arn::lambda_network_connector("network-connector-id")
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
lambda_network_connector(network_connector_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `network_connector_id` (String) Value for `${NetworkConnectorId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "kinesisvideo_stream function - arn"
subcategory: ""
description: |-
  kinesisvideo stream ARN
---

# function: kinesisvideo_stream

Builds the ARN for `kinesisvideo` `stream`:

```
arn:${Partition}:kinesisvideo:${Region}:${Account}:stream/${StreamName}/${CreationTime}
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.

## Example Usage

```terraform
# arn:aws:kinesisvideo:ap-northeast-1:111111111111:stream/stream-name/creation-time
output "kinesisvideo_stream" {
  value = provider::arn::kinesisvideo_stream("stream-name", "creation-time")
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
kinesisvideo_stream(stream_name string, creation_time string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `stream_name` (String) Value for `${StreamName}` in the ARN template.
1. `creation_time` (String) Value for `${CreationTime}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

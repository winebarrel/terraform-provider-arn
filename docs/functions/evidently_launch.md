---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "evidently_launch function - arn"
subcategory: ""
description: |-
  Builds an evidently Launch ARN
---

# function: evidently_launch

Builds the ARN of an `evidently` `Launch`:

```
arn:${Partition}:evidently:${Region}:${Account}:project/${ProjectName}/launch/${LaunchName}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
evidently_launch(project_name string, launch_name string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `project_name` (String) Value for `${ProjectName}` in the ARN template.
1. `launch_name` (String) Value for `${LaunchName}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given.

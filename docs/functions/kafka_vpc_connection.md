---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "kafka_vpc_connection function - arn"
subcategory: ""
description: |-
  kafka vpc-connection ARN
---

# function: kafka_vpc_connection

Builds the ARN for `kafka` `vpc-connection`:

```
arn:${Partition}:kafka:${Region}:${VpcOwnerAccount}:vpc-connection/${ClusterOwnerAccount}/${ClusterName}/${Uuid}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
kafka_vpc_connection(vpc_owner_account string, cluster_owner_account string, cluster_name string, uuid string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `vpc_owner_account` (String) Value for `${VpcOwnerAccount}` in the ARN template.
1. `cluster_owner_account` (String) Value for `${ClusterOwnerAccount}` in the ARN template.
1. `cluster_name` (String) Value for `${ClusterName}` in the ARN template.
1. `uuid` (String) Value for `${Uuid}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

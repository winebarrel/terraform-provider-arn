---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "braket_device function - arn"
subcategory: ""
description: |-
  Builds an braket device ARN
---

# function: braket_device

Builds the ARN of an `braket` `device`:

```
arn:${Partition}:braket:*:*:device/${DeviceType}/${Provider}/${DeviceId}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
braket_device(device_type string, provider string, device_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `device_type` (String) Value for `${DeviceType}` in the ARN template.
1. `provider` (String) Value for `${Provider}` in the ARN template.
1. `device_id` (String) Value for `${DeviceId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given.

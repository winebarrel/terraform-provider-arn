---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "sso_trusted_token_issuer function - arn"
subcategory: ""
description: |-
  Builds an sso TrustedTokenIssuer ARN
---

# function: sso_trusted_token_issuer

Builds the ARN of an `sso` `TrustedTokenIssuer`:

```
arn:${Partition}:sso::${AccountId}:trustedTokenIssuer/${InstanceId}/${TrustedTokenIssuerId}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
sso_trusted_token_issuer(account_id string, instance_id string, trusted_token_issuer_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `account_id` (String) Value for `${AccountId}` in the ARN template.
1. `instance_id` (String) Value for `${InstanceId}` in the ARN template.
1. `trusted_token_issuer_id` (String) Value for `${TrustedTokenIssuerId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given.

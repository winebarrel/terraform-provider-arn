---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "dynamodb_import function - arn"
subcategory: ""
description: |-
  Builds an dynamodb import ARN
---

# function: dynamodb_import

Builds the ARN of an `dynamodb` `import`:

```
arn:${Partition}:dynamodb:${Region}:${Account}:table/${TableName}/import/${ImportName}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
dynamodb_import(table_name string, import_name string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `table_name` (String) Value for `${TableName}` in the ARN template.
1. `import_name` (String) Value for `${ImportName}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given.

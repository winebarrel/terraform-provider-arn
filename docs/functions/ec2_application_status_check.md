---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "ec2_application_status_check function - arn"
subcategory: ""
description: |-
  Builds an ec2 application-status-check ARN
---

# function: ec2_application_status_check

Builds the ARN of an `ec2` `application-status-check`:

```
arn:${Partition}:ec2:${Region}:${Account}:application-status-check/${ApplicationStatusCheckId}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
ec2_application_status_check(application_status_check_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `application_status_check_id` (String) Value for `${ApplicationStatusCheckId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given.

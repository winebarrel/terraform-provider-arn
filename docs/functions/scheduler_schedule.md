---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "scheduler_schedule function - arn"
subcategory: ""
description: |-
  scheduler schedule ARN
---

# function: scheduler_schedule

Builds the ARN for `scheduler` `schedule`:

```
arn:${Partition}:scheduler:${Region}:${Account}:schedule/${GroupName}/${ScheduleName}
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.

## Example Usage

```terraform
# arn:aws:scheduler:ap-northeast-1:111111111111:schedule/group-name/schedule-name
output "scheduler_schedule" {
  value = provider::arn::scheduler_schedule("group-name", "schedule-name")
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
scheduler_schedule(group_name string, schedule_name string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `group_name` (String) Value for `${GroupName}` in the ARN template.
1. `schedule_name` (String) Value for `${ScheduleName}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

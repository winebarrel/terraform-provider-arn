---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "workspaces_web_trust_store function - arn"
subcategory: ""
description: |-
  workspaces-web trustStore ARN
---

# function: workspaces_web_trust_store

Builds the ARN for `workspaces-web` `trustStore`:

```
arn:${Partition}:workspaces-web:${Region}:${Account}:trustStore/${TrustStoreId}
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.

## Example Usage

```terraform
# arn:aws:workspaces-web:ap-northeast-1:111111111111:trustStore/trust-store-id
output "workspaces_web_trust_store" {
  value = provider::arn::workspaces_web_trust_store("trust-store-id")
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
workspaces_web_trust_store(trust_store_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `trust_store_id` (String) Value for `${TrustStoreId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "backup_report_plan function - arn"
subcategory: ""
description: |-
  Builds an backup reportPlan ARN
---

# function: backup_report_plan

Builds the ARN of an `backup` `reportPlan`:

```
arn:${Partition}:backup:${Region}:${Account}:report-plan:${ReportPlanName}-${ReportPlanId}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
backup_report_plan(report_plan_name string, report_plan_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `report_plan_name` (String) Value for `${ReportPlanName}` in the ARN template.
1. `report_plan_id` (String) Value for `${ReportPlanId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given.

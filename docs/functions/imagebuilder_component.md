---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "imagebuilder_component function - arn"
subcategory: ""
description: |-
  Builds an imagebuilder component ARN
---

# function: imagebuilder_component

Builds the ARN of an `imagebuilder` `component`:

```
arn:${Partition}:imagebuilder:${Region}:${Account}:component/${ComponentName}/${ComponentVersion}/${ComponentBuildVersion}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
imagebuilder_component(component_name string, component_version string, component_build_version string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `component_name` (String) Value for `${ComponentName}` in the ARN template.
1. `component_version` (String) Value for `${ComponentVersion}` in the ARN template.
1. `component_build_version` (String) Value for `${ComponentBuildVersion}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

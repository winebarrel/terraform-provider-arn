---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "config_connector function - arn"
subcategory: ""
description: |-
  Builds an config Connector ARN
---

# function: config_connector

Builds the ARN of an `config` `Connector`:

```
arn:${Partition}:config:${Region}:${Account}:connector/${Provider}/${ProviderId}/${ConnectorId}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
config_connector(provider string, provider_id string, connector_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `provider` (String) Value for `${Provider}` in the ARN template.
1. `provider_id` (String) Value for `${ProviderId}` in the ARN template.
1. `connector_id` (String) Value for `${ConnectorId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

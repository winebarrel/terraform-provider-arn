---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "bedrock_agentcore_runtime_endpoint function - arn"
subcategory: ""
description: |-
  Builds an bedrock-agentcore runtime-endpoint ARN
---

# function: bedrock_agentcore_runtime_endpoint

Builds the ARN of an `bedrock-agentcore` `runtime-endpoint`:

```
arn:${Partition}:bedrock-agentcore:${Region}:${Account}:runtime/${RuntimeId}/runtime-endpoint/${Name}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
bedrock_agentcore_runtime_endpoint(runtime_id string, name string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `runtime_id` (String) Value for `${RuntimeId}` in the ARN template.
1. `name` (String) Value for `${Name}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

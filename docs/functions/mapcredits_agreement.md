---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "mapcredits_agreement function - arn"
subcategory: ""
description: |-
  mapcredits agreement ARN
---

# function: mapcredits_agreement

Builds the ARN for `mapcredits` `agreement`:

```
arn:${Partition}:mapcredits:::${Agreement}/${AgreementId}
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.

## Example Usage

```terraform
# arn:aws:mapcredits:::agreement/agreement-id
output "mapcredits_agreement" {
  value = provider::arn::mapcredits_agreement("agreement", "agreement-id")
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
mapcredits_agreement(agreement string, agreement_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `agreement` (String) Value for `${Agreement}` in the ARN template.
1. `agreement_id` (String) Value for `${AgreementId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

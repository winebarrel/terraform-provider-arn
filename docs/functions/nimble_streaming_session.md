---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "nimble_streaming_session function - arn"
subcategory: ""
description: |-
  Builds an nimble streaming-session ARN
---

# function: nimble_streaming_session

Builds the ARN of an `nimble` `streaming-session`:

```
arn:${Partition}:nimble:${Region}:${Account}:streaming-session/${StreamingSessionId}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
nimble_streaming_session(streaming_session_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `streaming_session_id` (String) Value for `${StreamingSessionId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given.

---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "apprunner_vpcconnector function - arn"
subcategory: ""
description: |-
  apprunner vpcconnector ARN
---

# function: apprunner_vpcconnector

Builds the ARN for `apprunner` `vpcconnector`:

```
arn:${Partition}:apprunner:${Region}:${Account}:vpcconnector/${VpcConnectorName}/${VpcConnectorVersion}/${VpcConnectorId}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
apprunner_vpcconnector(vpc_connector_name string, vpc_connector_version string, vpc_connector_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `vpc_connector_name` (String) Value for `${VpcConnectorName}` in the ARN template.
1. `vpc_connector_version` (String) Value for `${VpcConnectorVersion}` in the ARN template.
1. `vpc_connector_id` (String) Value for `${VpcConnectorId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

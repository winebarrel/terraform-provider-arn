---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "iq_payment_request function - arn"
subcategory: ""
description: |-
  Builds an iq paymentRequest ARN
---

# function: iq_payment_request

Builds the ARN of an `iq` `paymentRequest`:

```
arn:${Partition}:iq:${Region}::paymentRequest/${ConversationId}/${ProposalId}/${PaymentRequestId}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
iq_payment_request(conversation_id string, proposal_id string, payment_request_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `conversation_id` (String) Value for `${ConversationId}` in the ARN template.
1. `proposal_id` (String) Value for `${ProposalId}` in the ARN template.
1. `payment_request_id` (String) Value for `${PaymentRequestId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

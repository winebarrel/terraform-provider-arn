---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "cleanrooms_privacybudgettemplate function - arn"
subcategory: ""
description: |-
  Builds an cleanrooms privacybudgettemplate ARN
---

# function: cleanrooms_privacybudgettemplate

Builds the ARN of an `cleanrooms` `privacybudgettemplate`:

```
arn:${Partition}:cleanrooms:${Region}:${Account}:membership/${MembershipId}/privacybudgettemplate/${PrivacyBudgetTemplateId}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
cleanrooms_privacybudgettemplate(membership_id string, privacy_budget_template_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `membership_id` (String) Value for `${MembershipId}` in the ARN template.
1. `privacy_budget_template_id` (String) Value for `${PrivacyBudgetTemplateId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

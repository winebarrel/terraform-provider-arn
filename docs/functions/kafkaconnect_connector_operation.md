---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "kafkaconnect_connector_operation function - arn"
subcategory: ""
description: |-
  kafkaconnect connector operation ARN
---

# function: kafkaconnect_connector_operation

Builds the ARN for `kafkaconnect` `connector operation`:

```
arn:${Partition}:kafkaconnect:${Region}:${Account}:connector-operation/${ConnectorName}/${ConnectorUUID}/${UUID}
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.

## Example Usage

```terraform
# arn:aws:kafkaconnect:ap-northeast-1:111111111111:connector-operation/connector-name/connector-uuid/uuid
output "kafkaconnect_connector_operation" {
  value = provider::arn::kafkaconnect_connector_operation("connector-name", "connector-uuid", "uuid")
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
kafkaconnect_connector_operation(connector_name string, connector_uuid string, uuid string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `connector_name` (String) Value for `${ConnectorName}` in the ARN template.
1. `connector_uuid` (String) Value for `${ConnectorUUID}` in the ARN template.
1. `uuid` (String) Value for `${UUID}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "securityhub_connector function - arn"
subcategory: ""
description: |-
  securityhub connector ARN
---

# function: securityhub_connector

Builds the ARN for `securityhub` `connector`:

```
arn:${Partition}:securityhub:${Region}:${Account}:connector/${ConnectorId}
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.

## Example Usage

```terraform
# arn:aws:securityhub:ap-northeast-1:111111111111:connector/connector-id
output "securityhub_connector" {
  value = provider::arn::securityhub_connector("connector-id")
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
securityhub_connector(connector_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `connector_id` (String) Value for `${ConnectorId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

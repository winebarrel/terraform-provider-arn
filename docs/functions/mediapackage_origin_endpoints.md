---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "mediapackage_origin_endpoints function - arn"
subcategory: ""
description: |-
  Builds an mediapackage origin_endpoints ARN
---

# function: mediapackage_origin_endpoints

Builds the ARN of an `mediapackage` `origin_endpoints`:

```
arn:${Partition}:mediapackage:${Region}:${Account}:origin_endpoints/${OriginEndpointIdentifier}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
mediapackage_origin_endpoints(origin_endpoint_identifier string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `origin_endpoint_identifier` (String) Value for `${OriginEndpointIdentifier}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given.

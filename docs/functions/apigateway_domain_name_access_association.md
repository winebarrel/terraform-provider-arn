---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "apigateway_domain_name_access_association function - arn"
subcategory: ""
description: |-
  apigateway DomainNameAccessAssociation ARN
---

# function: apigateway_domain_name_access_association

Builds the ARN for `apigateway` `DomainNameAccessAssociation`:

```
arn:${Partition}:apigateway:${Region}:${Account}:/domainnameaccessassociations/domainname/${DomainName}/${SourceType}/${SourceId}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
apigateway_domain_name_access_association(domain_name string, source_type string, source_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `domain_name` (String) Value for `${DomainName}` in the ARN template.
1. `source_type` (String) Value for `${SourceType}` in the ARN template.
1. `source_id` (String) Value for `${SourceId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

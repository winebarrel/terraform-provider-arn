---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "lambda_microvm_image function - arn"
subcategory: ""
description: |-
  lambda microvmImage ARN
---

# function: lambda_microvm_image

Builds the ARN for `lambda` `microvmImage`:

```
arn:${Partition}:lambda:${Region}:${Account}:microvm-image:${MicrovmImageName}
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.

## Example Usage

```terraform
# arn:aws:lambda:ap-northeast-1:111111111111:microvm-image:microvm-image-name
output "lambda_microvm_image" {
  value = provider::arn::lambda_microvm_image("microvm-image-name")
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
lambda_microvm_image(microvm_image_name string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `microvm_image_name` (String) Value for `${MicrovmImageName}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

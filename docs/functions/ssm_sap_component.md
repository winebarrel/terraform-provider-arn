---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "ssm_sap_component function - arn"
subcategory: ""
description: |-
  ssm-sap component ARN
---

# function: ssm_sap_component

Builds the ARN for `ssm-sap` `component`:

```
arn:${Partition}:ssm-sap:${Region}:${Account}:${ApplicationType}/${ApplicationId}/COMPONENT/${ComponentId}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
ssm_sap_component(application_type string, application_id string, component_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `application_type` (String) Value for `${ApplicationType}` in the ARN template.
1. `application_id` (String) Value for `${ApplicationId}` in the ARN template.
1. `component_id` (String) Value for `${ComponentId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

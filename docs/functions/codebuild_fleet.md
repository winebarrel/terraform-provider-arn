---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "codebuild_fleet function - arn"
subcategory: ""
description: |-
  Builds an codebuild fleet ARN
---

# function: codebuild_fleet

Builds the ARN of an `codebuild` `fleet`:

```
arn:${Partition}:codebuild:${Region}:${Account}:fleet/${FleetName}:${FleetId}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
codebuild_fleet(fleet_name string, fleet_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `fleet_name` (String) Value for `${FleetName}` in the ARN template.
1. `fleet_id` (String) Value for `${FleetId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given.

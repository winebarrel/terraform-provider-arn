---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "app_integrations_data_integration function - arn"
subcategory: ""
description: |-
  Builds an app-integrations data-integration ARN
---

# function: app_integrations_data_integration

Builds the ARN of an `app-integrations` `data-integration`:

```
arn:${Partition}:app-integrations:${Region}:${Account}:data-integration/${DataIntegrationId}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
app_integrations_data_integration(data_integration_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `data_integration_id` (String) Value for `${DataIntegrationId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given.

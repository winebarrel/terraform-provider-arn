---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "bedrock_data_automation_library_ingestion_job function - arn"
subcategory: ""
description: |-
  bedrock data-automation-library-ingestion-job ARN
---

# function: bedrock_data_automation_library_ingestion_job

Builds the ARN for `bedrock` `data-automation-library-ingestion-job`:

```
arn:${Partition}:bedrock:${Region}:${Account}:data-automation-library-ingestion-job/${IngestionJobId}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
bedrock_data_automation_library_ingestion_job(ingestion_job_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `ingestion_job_id` (String) Value for `${IngestionJobId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

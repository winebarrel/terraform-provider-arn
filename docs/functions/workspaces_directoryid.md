---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "workspaces_directoryid function - arn"
subcategory: ""
description: |-
  workspaces directoryid ARN
---

# function: workspaces_directoryid

Builds the ARN for `workspaces` `directoryid`:

```
arn:${Partition}:workspaces:${Region}:${Account}:directory/${DirectoryId}
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.

## Example Usage

```terraform
# arn:aws:workspaces:ap-northeast-1:111111111111:directory/directory-id
output "workspaces_directoryid" {
  value = provider::arn::workspaces_directoryid("directory-id")
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
workspaces_directoryid(directory_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `directory_id` (String) Value for `${DirectoryId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "iotsitewise_computation_model function - arn"
subcategory: ""
description: |-
  iotsitewise computation-model ARN
---

# function: iotsitewise_computation_model

Builds the ARN for `iotsitewise` `computation-model`:

```
arn:${Partition}:iotsitewise:${Region}:${Account}:computation-model/${ComputationModelId}
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.

## Example Usage

```terraform
# arn:aws:iotsitewise:ap-northeast-1:111111111111:computation-model/computation-model-id
output "iotsitewise_computation_model" {
  value = provider::arn::iotsitewise_computation_model("computation-model-id")
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
iotsitewise_computation_model(computation_model_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `computation_model_id` (String) Value for `${ComputationModelId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

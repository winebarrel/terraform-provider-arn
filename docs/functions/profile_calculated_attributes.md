---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "profile_calculated_attributes function - arn"
subcategory: ""
description: |-
  profile calculated-attributes ARN
---

# function: profile_calculated_attributes

Builds the ARN for `profile` `calculated-attributes`:

```
arn:${Partition}:profile:${Region}:${Account}:domains/${DomainName}/calculated-attributes/${CalculatedAttributeName}
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.

## Example Usage

```terraform
# arn:aws:profile:ap-northeast-1:111111111111:domains/domain-name/calculated-attributes/calculated-attribute-name
output "profile_calculated_attributes" {
  value = provider::arn::profile_calculated_attributes("domain-name", "calculated-attribute-name")
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
profile_calculated_attributes(domain_name string, calculated_attribute_name string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `domain_name` (String) Value for `${DomainName}` in the ARN template.
1. `calculated_attribute_name` (String) Value for `${CalculatedAttributeName}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

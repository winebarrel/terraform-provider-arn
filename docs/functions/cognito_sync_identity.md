---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "cognito_sync_identity function - arn"
subcategory: ""
description: |-
  Builds an cognito-sync identity ARN
---

# function: cognito_sync_identity

Builds the ARN of an `cognito-sync` `identity`:

```
arn:${Partition}:cognito-sync:${Region}:${Account}:identitypool/${IdentityPoolId}/identity/${IdentityId}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
cognito_sync_identity(identity_pool_id string, identity_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `identity_pool_id` (String) Value for `${IdentityPoolId}` in the ARN template.
1. `identity_id` (String) Value for `${IdentityId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given.

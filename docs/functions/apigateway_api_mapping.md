---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "apigateway_api_mapping function - arn"
subcategory: ""
description: |-
  apigateway ApiMapping ARN
---

# function: apigateway_api_mapping

Builds the ARN for `apigateway` `ApiMapping`:

```
arn:${Partition}:apigateway:${Region}::/domainnames/${DomainName}/apimappings/${ApiMappingId}
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.

## Example Usage

```terraform
# arn:aws:apigateway:ap-northeast-1::/domainnames/domain-name/apimappings/api-mapping-id
output "apigateway_api_mapping" {
  value = provider::arn::apigateway_api_mapping("domain-name", "api-mapping-id")
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
apigateway_api_mapping(domain_name string, api_mapping_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `domain_name` (String) Value for `${DomainName}` in the ARN template.
1. `api_mapping_id` (String) Value for `${ApiMappingId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

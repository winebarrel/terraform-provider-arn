---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "nova_act_workflow_run function - arn"
subcategory: ""
description: |-
  nova-act workflow-run ARN
---

# function: nova_act_workflow_run

Builds the ARN for `nova-act` `workflow-run`:

```
arn:${Partition}:nova-act:${Region}:${Account}:workflow-definition/${WorkflowDefinitionName}/workflow-run/${WorkflowRunId}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
nova_act_workflow_run(workflow_definition_name string, workflow_run_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `workflow_definition_name` (String) Value for `${WorkflowDefinitionName}` in the ARN template.
1. `workflow_run_id` (String) Value for `${WorkflowRunId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

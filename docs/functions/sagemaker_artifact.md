---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "sagemaker_artifact function - arn"
subcategory: ""
description: |-
  sagemaker artifact ARN
---

# function: sagemaker_artifact

Builds the ARN for `sagemaker` `artifact`:

```
arn:${Partition}:sagemaker:${Region}:${Account}:artifact/${HashOfArtifactSource}
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.

## Example Usage

```terraform
# arn:aws:sagemaker:ap-northeast-1:111111111111:artifact/hash-of-artifact-source
output "sagemaker_artifact" {
  value = provider::arn::sagemaker_artifact("hash-of-artifact-source")
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
sagemaker_artifact(hash_of_artifact_source string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `hash_of_artifact_source` (String) Value for `${HashOfArtifactSource}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "acm_acme_domain_validation function - arn"
subcategory: ""
description: |-
  acm acme-domain-validation ARN
---

# function: acm_acme_domain_validation

Builds the ARN for `acm` `acme-domain-validation`:

```
arn:${Partition}:acm:${Region}:${Account}:acme-endpoint/${AcmeEndpointId}/acme-domain-validation/${AcmeDomainValidationId}
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.

## Example Usage

```terraform
# arn:aws:acm:ap-northeast-1:111111111111:acme-endpoint/acme-endpoint-id/acme-domain-validation/acme-domain-validation-id
output "acm_acme_domain_validation" {
  value = provider::arn::acm_acme_domain_validation("acme-endpoint-id", "acme-domain-validation-id")
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
acm_acme_domain_validation(acme_endpoint_id string, acme_domain_validation_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `acme_endpoint_id` (String) Value for `${AcmeEndpointId}` in the ARN template.
1. `acme_domain_validation_id` (String) Value for `${AcmeDomainValidationId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "greengrass_deployment function - arn"
subcategory: ""
description: |-
  Builds an greengrass deployment ARN
---

# function: greengrass_deployment

Builds the ARN of an `greengrass` `deployment`:

```
arn:${Partition}:greengrass:${Region}:${Account}:/greengrass/groups/${GroupId}/deployments/${DeploymentId}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
greengrass_deployment(group_id string, deployment_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `group_id` (String) Value for `${GroupId}` in the ARN template.
1. `deployment_id` (String) Value for `${DeploymentId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given.

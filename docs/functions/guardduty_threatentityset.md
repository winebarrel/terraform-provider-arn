---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "guardduty_threatentityset function - arn"
subcategory: ""
description: |-
  Builds an guardduty threatentityset ARN
---

# function: guardduty_threatentityset

Builds the ARN of an `guardduty` `threatentityset`:

```
arn:${Partition}:guardduty:${Region}:${Account}:detector/${DetectorId}/threatentityset/${ThreatEntitySetId}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
guardduty_threatentityset(detector_id string, threat_entity_set_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `detector_id` (String) Value for `${DetectorId}` in the ARN template.
1. `threat_entity_set_id` (String) Value for `${ThreatEntitySetId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "sagemaker_endpoint_config function - arn"
subcategory: ""
description: |-
  sagemaker endpoint-config ARN
---

# function: sagemaker_endpoint_config

Builds the ARN for `sagemaker` `endpoint-config`:

```
arn:${Partition}:sagemaker:${Region}:${Account}:endpoint-config/${EndpointConfigName}
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.

## Example Usage

```terraform
# arn:aws:sagemaker:ap-northeast-1:111111111111:endpoint-config/endpoint-config-name
output "sagemaker_endpoint_config" {
  value = provider::arn::sagemaker_endpoint_config("endpoint-config-name")
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
sagemaker_endpoint_config(endpoint_config_name string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `endpoint_config_name` (String) Value for `${EndpointConfigName}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

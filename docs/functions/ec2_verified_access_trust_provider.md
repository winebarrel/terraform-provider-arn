---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "ec2_verified_access_trust_provider function - arn"
subcategory: ""
description: |-
  Builds an ec2 verified-access-trust-provider ARN
---

# function: ec2_verified_access_trust_provider

Builds the ARN of an `ec2` `verified-access-trust-provider`:

```
arn:${Partition}:ec2:${Region}:${Account}:verified-access-trust-provider/${VerifiedAccessTrustProviderId}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
ec2_verified_access_trust_provider(verified_access_trust_provider_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `verified_access_trust_provider_id` (String) Value for `${VerifiedAccessTrustProviderId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

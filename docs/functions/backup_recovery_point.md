---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "backup_recovery_point function - arn"
subcategory: ""
description: |-
  Builds an backup recoveryPoint ARN
---

# function: backup_recovery_point

Builds the ARN of an `backup` `recoveryPoint`:

```
arn:${Partition}:${Vendor}:${Region}:*:${ResourceType}:${RecoveryPointId}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
backup_recovery_point(vendor string, resource_type string, recovery_point_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `vendor` (String) Value for `${Vendor}` in the ARN template.
1. `resource_type` (String) Value for `${ResourceType}` in the ARN template.
1. `recovery_point_id` (String) Value for `${RecoveryPointId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given.

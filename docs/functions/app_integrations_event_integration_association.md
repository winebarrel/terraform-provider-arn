---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "app_integrations_event_integration_association function - arn"
subcategory: ""
description: |-
  Builds an app-integrations event-integration-association ARN
---

# function: app_integrations_event_integration_association

Builds the ARN of an `app-integrations` `event-integration-association`:

```
arn:${Partition}:app-integrations:${Region}:${Account}:event-integration-association/${EventIntegrationName}/${ResourceId}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
app_integrations_event_integration_association(event_integration_name string, resource_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `event_integration_name` (String) Value for `${EventIntegrationName}` in the ARN template.
1. `resource_id` (String) Value for `${ResourceId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given.

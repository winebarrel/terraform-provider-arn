---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "chime_channel_flow function - arn"
subcategory: ""
description: |-
  Builds an chime channel-flow ARN
---

# function: chime_channel_flow

Builds the ARN of an `chime` `channel-flow`:

```
arn:${Partition}:chime:${Region}:${AccountId}:app-instance/${AppInstanceId}/channel-flow/${ChannelFlowId}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
chime_channel_flow(account_id string, app_instance_id string, channel_flow_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `account_id` (String) Value for `${AccountId}` in the ARN template.
1. `app_instance_id` (String) Value for `${AppInstanceId}` in the ARN template.
1. `channel_flow_id` (String) Value for `${ChannelFlowId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

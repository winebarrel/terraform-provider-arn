---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "qldb_stream function - arn"
subcategory: ""
description: |-
  Builds an qldb stream ARN
---

# function: qldb_stream

Builds the ARN of an `qldb` `stream`:

```
arn:${Partition}:qldb:${Region}:${Account}:stream/${LedgerName}/${StreamId}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
qldb_stream(ledger_name string, stream_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `ledger_name` (String) Value for `${LedgerName}` in the ARN template.
1. `stream_id` (String) Value for `${StreamId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given.

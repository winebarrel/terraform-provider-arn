---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "eks_capability function - arn"
subcategory: ""
description: |-
  Builds an eks capability ARN
---

# function: eks_capability

Builds the ARN of an `eks` `capability`:

```
arn:${Partition}:eks:${Region}:${Account}:capability/${ClusterName}/${CapabilityType}/${CapabilityName}/${UUID}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
eks_capability(cluster_name string, capability_type string, capability_name string, uuid string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `cluster_name` (String) Value for `${ClusterName}` in the ARN template.
1. `capability_type` (String) Value for `${CapabilityType}` in the ARN template.
1. `capability_name` (String) Value for `${CapabilityName}` in the ARN template.
1. `uuid` (String) Value for `${UUID}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given.

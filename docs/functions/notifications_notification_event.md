---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "notifications_notification_event function - arn"
subcategory: ""
description: |-
  Builds an notifications NotificationEvent ARN
---

# function: notifications_notification_event

Builds the ARN of an `notifications` `NotificationEvent`:

```
arn:${Partition}:notifications:${Region}:${Account}:configuration/${NotificationConfigurationId}/event/${NotificationEventId}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
notifications_notification_event(notification_configuration_id string, notification_event_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `notification_configuration_id` (String) Value for `${NotificationConfigurationId}` in the ARN template.
1. `notification_event_id` (String) Value for `${NotificationEventId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "kafkaconnect_worker_configuration function - arn"
subcategory: ""
description: |-
  Builds an kafkaconnect worker configuration ARN
---

# function: kafkaconnect_worker_configuration

Builds the ARN of an `kafkaconnect` `worker configuration`:

```
arn:${Partition}:kafkaconnect:${Region}:${Account}:worker-configuration/${WorkerConfigurationName}/${UUID}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
kafkaconnect_worker_configuration(worker_configuration_name string, uuid string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `worker_configuration_name` (String) Value for `${WorkerConfigurationName}` in the ARN template.
1. `uuid` (String) Value for `${UUID}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given.

---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "organizations_resourcepolicy function - arn"
subcategory: ""
description: |-
  organizations resourcepolicy ARN
---

# function: organizations_resourcepolicy

Builds the ARN for `organizations` `resourcepolicy`:

```
arn:${Partition}:organizations::${Account}:resourcepolicy/o-${OrganizationId}/rp-${ResourcePolicyId}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
organizations_resourcepolicy(organization_id string, resource_policy_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `organization_id` (String) Value for `${OrganizationId}` in the ARN template.
1. `resource_policy_id` (String) Value for `${ResourcePolicyId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

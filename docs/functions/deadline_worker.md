---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "deadline_worker function - arn"
subcategory: ""
description: |-
  Builds an deadline worker ARN
---

# function: deadline_worker

Builds the ARN of an `deadline` `worker`:

```
arn:${Partition}:deadline:${Region}:${Account}:farm/${FarmId}/fleet/${FleetId}/worker/${WorkerId}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
deadline_worker(farm_id string, fleet_id string, worker_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `farm_id` (String) Value for `${FarmId}` in the ARN template.
1. `fleet_id` (String) Value for `${FleetId}` in the ARN template.
1. `worker_id` (String) Value for `${WorkerId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given.

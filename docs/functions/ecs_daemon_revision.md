---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "ecs_daemon_revision function - arn"
subcategory: ""
description: |-
  ecs daemon-revision ARN
---

# function: ecs_daemon_revision

Builds the ARN for `ecs` `daemon-revision`:

```
arn:${Partition}:ecs:${Region}:${Account}:daemon-revision/${ClusterName}/${DaemonName}/${DaemonRevisionId}
```

`${Partition}`, `${Region}` and `${Account}` come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.



## Signature

<!-- signature generated by tfplugindocs -->
```text
ecs_daemon_revision(cluster_name string, daemon_name string, daemon_revision_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `cluster_name` (String) Value for `${ClusterName}` in the ARN template.
1. `daemon_name` (String) Value for `${DaemonName}` in the ARN template.
1. `daemon_revision_id` (String) Value for `${DaemonRevisionId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "identity_sync_sync_target_resource function - arn"
subcategory: ""
description: |-
  identity-sync SyncTargetResource ARN
---

# function: identity_sync_sync_target_resource

Builds the ARN for `identity-sync` `SyncTargetResource`:

```
arn:${Partition}:identity-sync:${Region}:${Account}:target/${SyncProfileName}/${SyncTargetName}
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.

## Example Usage

```terraform
# arn:aws:identity-sync:ap-northeast-1:111111111111:target/sync-profile-name/sync-target-name
output "identity_sync_sync_target_resource" {
  value = provider::arn::identity_sync_sync_target_resource("sync-profile-name", "sync-target-name")
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
identity_sync_sync_target_resource(sync_profile_name string, sync_target_name string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `sync_profile_name` (String) Value for `${SyncProfileName}` in the ARN template.
1. `sync_target_name` (String) Value for `${SyncTargetName}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.

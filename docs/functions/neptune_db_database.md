---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "neptune_db_database function - arn"
subcategory: ""
description: |-
  neptune-db database ARN
---

# function: neptune_db_database

Builds the ARN for `neptune-db` `database`:

```
arn:${Partition}:neptune-db:${Region}:${Account}:${ClusterResourceId}/*
```

The partition, region and account fields come from the configuration file (`.arn.hcl`, or the path in `ARN_CONFIG`). The remaining placeholders are the arguments, in template order.

## Example Usage

```terraform
# arn:aws:neptune-db:ap-northeast-1:111111111111:cluster-resource-id/*
output "neptune_db_database" {
  value = provider::arn::neptune_db_database("cluster-resource-id")
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
neptune_db_database(cluster_resource_id string, options map of string...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `cluster_resource_id` (String) Value for `${ClusterResourceId}` in the ARN template.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Map of String) Optional overrides: `account` (the name of an `account` block in the configuration file), `account_id` (a literal account id), `region` and `partition`. At most one options map may be given. An unrecognized key, or a value set to an empty string, is an error; omit an option, or set it to `null`, to use the default.
